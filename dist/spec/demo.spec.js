describe("Just a test", () => {
    let a;
    it("should be true", () => {
        a = true;
        expect(a).toBe(true);
    });
    it("will fail", () => {
        expect(a).toBe(false);
    });
});
//# sourceMappingURL=demo.spec.js.map